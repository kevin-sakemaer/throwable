## 1.0.0-alpha.2

- Add `TargetKind.field` and `TargetKind.topLevelVariable` to `@Throws`, allowing the annotation on fields and top-level variables.

## 1.0.0-alpha.1

- Initial release of the `@Throws` annotation for declaring checked exceptions in Dart.
